import React from 'react'
import styled from 'styled-components'

const CardSwitcher = ({array}) => {
  return (
    <DIV>

    </DIV>
  )
}

export default CardSwitcher

const DIV = styled.div`
  display: flex;
  gap: 20px;
  overflow-x: auto;

`