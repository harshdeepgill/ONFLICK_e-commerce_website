
const initState = {
    isLoading: false,
    isError: false,
    products: []
}

export const reducer = (state = initState, {type, paylaod})=>{
    switch(type){
        default: return state
    }
}